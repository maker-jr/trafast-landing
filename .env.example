# Server-side only — read by netlify/functions/waitlist.ts.
# Set these in Netlify under Site configuration > Environment variables.
# Never prefix them with VITE_, or Vite will inline them into the public bundle.

RESEND_API_KEY=
RESEND_AUDIENCE_ID=
